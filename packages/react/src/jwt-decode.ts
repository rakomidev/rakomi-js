/**
 * JWT payload decoder — re-export from `@rakomi/sdk-core`.
 * Single source of truth shared with `@rakomi/react-native`. Behavior unchanged.
 */
export { decodeJwtPayload, decodeSession, decodeUser } from '@rakomi/sdk-core';
